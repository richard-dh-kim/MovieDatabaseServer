Richard Kim
101138475
COMP2406A
Final Project

Instructions:
1) unzip the zip file
2) travel to the directory in cmd
3) create a directory (folder) where the database will stay (call it database).
4) use the command 'npm install' to install all dependencies using package.json
5) run mongod using command "mongod --dbpath=database"
6) if mongod is running successfully, open a second command, travel to the directory.
7) run the command "npm run start" to initialize the database, and start the server
8) go to http://localhost:3000/ to start navigating the web application

Report is called "RichardKim_101138475_FinalReport"
